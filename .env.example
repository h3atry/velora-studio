# Credenciais OAuth do APP (como OBS) — copie oauth-credentials.json.example → oauth-credentials.json
# Usuário final NÃO precisa deste arquivo se o build já embarcar oauth-credentials.json.
# Redirect Twitch: https://127.0.0.1:17563/callback/twitch
# Redirect TikTok: https://127.0.0.1:17564/callback/tiktok

# Fallback dev (opcional se usar oauth-credentials.json):
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
